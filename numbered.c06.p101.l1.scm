(define numbered?
  (lambda (aexp)
    (cond
      (___ ___)
      (___ ___)
      (___ ___))))


; stub

; > (numbered? 1)
; #t
; > (numbered? `(3 o+ (4 oexpt 5)))
; #t
; > (numbered? `(2 omultiply sausage))
; #f
